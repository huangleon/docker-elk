<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<source>
@type syslog
port 5140
bind 0.0.0.0
tag system
</source>

<match **>
  @type elasticsearch
  host ${LOGGING_ELASTICSEARCH_SERVICE_HOST}
  port ${LOGGING_ELASTICSEARCH_SERVICE_PORT}
  logstash_format true
</match>
